namespace Workshop.Api;

/// <summary>
///     Constants representing the names of properties in the appsettings.json file.
/// </summary>
public class WorkshopConstants
{
    /// <summary>
    ///     Name of the property in the appsettings.json file.
    /// </summary>
    public const string WorkshopOptions = nameof(WorkshopOptions);

    /// <summary>
    ///     Name of the property in the appsettings.json file.
    /// </summary>
    public const string DefaultConnection = nameof(DefaultConnection);

    /// <summary>
    ///     Name of the property in the appsettings.json file.
    /// </summary>
    public const string ConnectionStringEnv = nameof(ConnectionStringEnv);
}
